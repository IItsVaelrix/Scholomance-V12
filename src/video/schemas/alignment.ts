// Alignment sidecar schema — canonical types re-exported from types.ts
// Spec path: src/video/schemas/alignment.ts
export type {
  WordTiming,
  AlignmentSidecar,
} from "../types";

export {
  validateAlignmentSidecar,
  validateWordTiming,
} from "../types";
